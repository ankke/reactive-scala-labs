package EShop.lab2

import EShop.lab3.TypedOrderManager
import akka.actor.typed.{ActorSystem}

object Main {

  def main(args: Array[String]): Unit = {
//    val orderManager = ActorSystem(new TypedOrderManager().start, "typed-system")
//    orderManager ! TypedOrderManager.AddItem("1", )
//    orderManager.terminate()
  }
}
